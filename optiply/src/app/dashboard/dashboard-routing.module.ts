import { Routes } from '@angular/router';
import { DashboardComponent } from './components';

export const DashboardRoutes: Routes = [
    {
        path: 'optiply-dashboard',
        component: DashboardComponent
    }
];
