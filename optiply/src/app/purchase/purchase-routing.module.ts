import { Routes } from '@angular/router';
import { PurchaseComponent } from './components';

export const PurchaseRoutes: Routes = [
    {
        path: 'optiply-purchase',
        component: PurchaseComponent
    }
];